---
import { Image } from 'astro:assets'
import buildRect from '#assets/Image_asset_764x535_2x.jpg'
import type { ArgumentsData } from '#types/arguments.ts'

interface Props {
	argumentsData: ArgumentsData
}

const { argumentsData } = Astro.props
---

<section id="Financing" class="mx-[4vw] mt-25 flex flex-col gap-20">
	<div
		class="flex flex-col gap-5 lg:grid lg:gap-0 grid-flow-col grid-cols-[45%_55%] items-center"
	>
		<div class="lg:px-[20%] text-justify">
			<h2 class="text-left">INVESTIČNÍ POTENCIÁL</h2>
			<p>
				{argumentsData.investment.text}
			</p>
		</div>
		<Image
			src={buildRect}
			alt="Pohled na objekt"
			class="max-h-[calc(80vh-144px)] aspect-video object-cover"
		/>
	</div>
	<div class="text-center">SEM PŘIJDE BROŽURA</div>
</section>
