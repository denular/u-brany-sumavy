export interface ArgumentContent {
	title: string
	text: string
}

export interface ArgumentsData {
	about: ArgumentContent
	aboutLocality: ArgumentContent
	locality: ArgumentContent
	investment: ArgumentContent
}
